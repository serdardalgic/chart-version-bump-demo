# Demo 7a: concurrent /merge (side A)

Part of a pair with demo-7b. Comment `/merge` on both this PR and
demo-7b in quick succession. Confirm only one run proceeds and the
other is rejected as "already in progress" (no queueing).
