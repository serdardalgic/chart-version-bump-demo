# Demo 7b: concurrent /merge (side B)

Part of a pair with demo-7a. Comment `/merge` on both this PR and
demo-7a in quick succession. Confirm only one run proceeds and the
other is rejected as "already in progress" (no queueing).
