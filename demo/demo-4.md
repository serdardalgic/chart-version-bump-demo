# Demo 4: conflicting labels

Apply both `no-version-bump` and `minor-bump` labels to this PR, then
comment `/merge`. Confirm it's rejected with an explanatory comment
(the labels are mutually exclusive) and nothing merges.
