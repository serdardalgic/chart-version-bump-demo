# Demo 3: merge conflicts

This PR's base branch (`test/demo-3`) was modified directly after this
branch was forked, on the same `## Unreleased` line, to force a merge
conflict. Comment `/merge` and confirm it's rejected as not mergeable.
