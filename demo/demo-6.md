# Demo 6: missing/empty Unreleased

This PR intentionally has no bullet under `## Unreleased` and no
`no-version-bump` label. Comment `/merge` and confirm it's rejected
with guidance to add a changelog bullet or apply the label.
