# Demo 2: no approval

This PR targets `main` directly (not `test/*`), so the approval check
is not bypassed. Comment `/merge` with zero approvals and confirm it's
rejected with an explanatory comment. Do not approve or merge this PR.
